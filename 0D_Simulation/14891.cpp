#include <bits/stdc++.h>
using namespace std;

string gears[4];
int K;
int target, cmd;

int main() {
    for (int i = 0; i < 4; i++) {
        cin >> gears[i];
    }

    cin >> K;
    while(K--) {
        cin >> target >> cmd;
        
    }
}